# SAFE-K8S-0301-004 - PSA exemption management for privileged system and GPU workloads

- Domain: D03 - Workload and Pod Security
- Knowledge Area: 3.1 - Pod Security Standards and Admission
- Maturity: Foundational
- Classification: ai-specific

## Statement

Organizations must maintain a formal exemption register for workloads that require elevated privileges beyond their namespace PSS level, including GPU device plugins, CNI agents, and storage drivers, and must enforce compensating controls for each exempted workload through policy engine rules scoped to specific service accounts, pod labels, or namespaces.

## Objective

Allow legitimate infrastructure workloads (GPU device plugins, CNI agents, node-level daemons) to operate with required privileges while preventing exemption abuse that would undermine Pod Security Standards enforcement.

## Rationale

GPU device plugins and CNI agents require host-level access that conflicts with Restricted or Baseline PSS levels. Without a formal exemption process, teams either weaken the entire namespace to Privileged -- exposing all co-located workloads -- or bypass admission controls entirely. Untracked exemptions accumulate over time and create hidden attack surfaces that auditors cannot assess.

## Scope

Applies to all workloads requesting privileges beyond their namespace PSS level, including GPU device plugins, CNI agents, CSI drivers, log collectors, and monitoring DaemonSets. Each exemption must specify the exact privileges required and the compensating controls applied.

## Enforcement

- Primary Enforcement Point: Policy engine (OPA Gatekeeper, Kyverno) exemption rules scoped to specific service accounts or pod labels, with PSA namespace-level exemption configuration for system workloads.
- Implementation Surface: Policy engines (OPA Gatekeeper, Kyverno) with exemption rules scoped to specific ServiceAccounts and pod labels, PSA namespace-level exemption configuration, formal exemption register in version control, NVIDIA device plugin DaemonSets (nvidia-device-plugin, GPU Operator, DCGM exporter, MIG manager), CNI agent DaemonSets (Cilium, Calico, Multus), CSI driver pods, network policies for compensating isolation, and audit logging at RequestResponse level for exempted namespaces.
- Owner Primary: Platform Engineering
- Owner Supporting: Security Engineering; AI/ML Platform team; Infrastructure team

## Related Crosswalks

No related crosswalk rows.
