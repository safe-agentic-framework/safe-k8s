# SAFE-K8S-0201-001 - Kubelet authentication, authorization, and anonymous access hardening

- Domain: D02 - Node, Runtime, and OS Security
- Knowledge Area: 2.1 - Kubelet and Node Configuration Hardening
- Maturity: Foundational
- Classification: baseline

## Statement

Production Kubernetes nodes must configure the kubelet to enforce authenticated and authorized access by enabling webhook authentication and authorization, disabling the read-only port (10255), and preventing anonymous access to the kubelet API.

## Objective

Reduce the node-level attack surface by ensuring that only authenticated and authorized entities can interact with the kubelet API, preventing unauthorized container manipulation or cluster reconnaissance.

## Rationale

The kubelet API provides direct access to pod lifecycle operations, container logs, and node status on each worker node. An unauthenticated or anonymous kubelet endpoint allows attackers who reach the node network to enumerate running workloads, exec into containers, or exfiltrate secrets without cluster credentials. Disabling the read-only port eliminates an information disclosure vector that bypasses all authorization checks.

## Scope

Applies to all kubelet instances on production cluster nodes, including dedicated GPU and AI inference nodes. For managed Kubernetes services where kubelet flags are provider-controlled, compensating controls such as node network isolation and provider-documented hardening attestation must be in place. Non-production clusters that process sensitive data or connect to production networks are also in scope.

## Enforcement

- Primary Enforcement Point: Node provisioning templates, kubelet configuration files, and configuration management tooling (e.g., Ansible, Terraform, cloud-init).
- Implementation Surface: KubeletConfiguration API settings (webhook authentication, webhook authorization, anonymous access, readOnlyPort), node provisioning templates, CIS Benchmark verification tooling (kube-bench), and kubelet configuration management automation.
- Owner Primary: Platform Engineering
- Owner Supporting: Security Engineering; Cloud Infrastructure

## Related Crosswalks

No related crosswalk rows.
